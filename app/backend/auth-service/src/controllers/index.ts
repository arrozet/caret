/**
 * Controllers for the Auth Service.
 * tsoa controllers are decorated classes that define HTTP routes and OpenAPI specs.
 * Each controller method: parses the request, validates the DTO, calls a Service, returns the response.
 *
 * Rule: no business logic inside controllers.
 * Rule: no direct Repository or ORM imports — delegate to Services.
 */
export {};

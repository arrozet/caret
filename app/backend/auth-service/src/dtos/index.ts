/**
 * Data Transfer Objects (DTOs) for the Auth Service.
 * DTOs define the exact shape of HTTP request bodies and response payloads.
 * They are validated automatically by tsoa at the Controller boundary.
 *
 * Rule: DTOs are only used in Controllers and in Service mapping logic.
 * Rule: DTOs must never be passed to Repositories — map them to Models first.
 */
export {};

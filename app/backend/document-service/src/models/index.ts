/**
 * Domain models for the Document Service.
 * These are pure TypeScript types representing internal business entities:
 * Document, Folder, Workspace, DocumentPermission.
 *
 * Rule: models are used inside Services and Repositories only.
 * Rule: never serialize models directly as HTTP responses — map them to DTOs first.
 */
export {};

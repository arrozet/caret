/**
 * Controllers for the Document Service.
 * tsoa-decorated classes that define REST routes for /api/v1/documents and /api/v1/workspaces.
 *
 * Rule: no business logic inside controllers.
 * Rule: no direct Repository or ORM imports — delegate to Services.
 */
export {};

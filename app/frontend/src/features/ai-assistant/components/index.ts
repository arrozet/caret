/**
 * AI Assistant feature components.
 * Presentation layer: ChatPanel, DiffView, StreamingText, ContextDisplay.
 * Components use accent-ai (orange) as per the design system.
 */
export { ChatPanel } from "./ChatPanel";

/**
 * Shared UI primitives (Presentation layer — "Dumb" components).
 * These components have NO business logic. They are styled via Tailwind
 * and fully controlled via props.
 *
 * Examples: Button, Input, Modal, Toast, Dropdown, Tabs.
 *
 * Rule: Do NOT import feature-specific hooks or stores here.
 */
export { Button } from "./Button";
export { Input } from "./Input";
